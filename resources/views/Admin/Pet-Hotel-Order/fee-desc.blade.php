<div class="text-center">
    <a
    data-pettype="{{$model->pet_type}}"
    data-petsize="{{$model->pet_size}}"
    data-slotavailable="{{$model->slot_available}}"
    data-priceperday="{{$model->price_per_day}}"
    data-toggle="modal" 
    data-target="#modal-detail-fee"
    class="btn btn-detail-fee mdi mdi-dots-horizontal text-primary fs-4"></a>
</div>